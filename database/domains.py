import workers

current = [workers.domain(9, 0)] * 14


def refresh():
    """
    Refresh the domains variable with the current user-inputted domains.

    Grabs data from actual UI.
    """
    pass
